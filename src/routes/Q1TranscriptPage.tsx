import React from 'react'
import TranscriptViewer from '../components/TranscriptViewer'

const Q1TranscriptPage: React.FC = () => {
  const wsUrl = 'mock'

  return <TranscriptViewer wsUrl={wsUrl} />
}

export default Q1TranscriptPage

